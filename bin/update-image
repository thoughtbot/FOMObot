#!/bin/sh

set -e

if [ "$(uname)" = Darwin ]; then
  docker-machine start dev || true
  eval "$(docker-machine env dev)"
fi

docker --debug=true --log-level=debug build --rm -t thoughtbot/fomobot .
docker tag -f thoughtbot/fomobot thoughtbot/fomobot:latest
docker push thoughtbot/fomobot
